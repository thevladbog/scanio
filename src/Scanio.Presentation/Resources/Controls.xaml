<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:settings="clr-namespace:Scanio.Presentation.Settings">
    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource Brush.SurfacePrimary}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="FontFamily" Value="{DynamicResource Font.Text}" />
        <Setter Property="FontSize" Value="{DynamicResource Type.Body}" />
    </Style>
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
    </Style>
    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource Font.Display}" />
        <Setter Property="FontSize" Value="{DynamicResource Type.Display}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style x:Key="PanelTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{DynamicResource Type.HeadingMedium}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style x:Key="KickerText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{DynamicResource Type.Kicker}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
    </Style>
    <Style x:Key="DataText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource Font.Data}" />
        <Setter Property="FontSize" Value="{DynamicResource Type.Data}" />
    </Style>
    <Style x:Key="NavigationButton" TargetType="RadioButton">
        <Setter Property="MinHeight" Value="{DynamicResource Size.ControlHeight}" />
        <Setter Property="Padding" Value="{DynamicResource Inset.3}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0,0,0,4" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
                <Setter Property="Background" Value="{DynamicResource Brush.SignalSurface}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.SignalSurface}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Signal}" />
                <Setter Property="BorderThickness" Value="0,0,0,4" />
                <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Brush.BorderDefault}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CommandButton" TargetType="Button">
        <Setter Property="MinHeight" Value="{DynamicResource Size.ControlHeight}" />
        <Setter Property="Padding" Value="{DynamicResource Inset.3}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfacePrimary}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderDefault}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource CommandButton}">
        <Setter Property="MinHeight" Value="{DynamicResource Size.PrimaryActionHeight}" />
        <Setter Property="Background" Value="{DynamicResource Brush.ActionPrimary}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.SurfacePrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.SurfacePrimary}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.ActionPrimaryHover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.ActionPrimaryPressed}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Signal}" />
                <Setter Property="BorderThickness" Value="2" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Brush.ActionDisabled}" />
                <Setter Property="Foreground" Value="{DynamicResource Brush.ActionTextDisabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.ActionDisabled}" />
                <Setter Property="BorderThickness" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource CommandButton}" />
    <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource CommandButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>
    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource CommandButton}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Error}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Error}" />
    </Style>
    <Style TargetType="TextBox">
        <Setter Property="MinHeight" Value="{DynamicResource Size.ControlHeight}" />
        <Setter Property="Padding" Value="{DynamicResource Inset.2}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderDefault}" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfacePrimary}" />
    </Style>
    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="{DynamicResource Size.ControlHeight}" />
        <Setter Property="Padding" Value="{DynamicResource Inset.2}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style TargetType="ComboBoxItem">
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style TargetType="ListViewItem">
        <Setter Property="Padding" Value="{DynamicResource Inset.3}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.SignalSurface}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Signal}" />
                <Setter Property="BorderThickness" Value="4,0,0,0" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="LedgerListBox" TargetType="ListBox">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
    </Style>
    <Style x:Key="LedgerRow" TargetType="ListBoxItem">
        <Setter Property="MinHeight" Value="{DynamicResource Size.LedgerRowCompact}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="4,0,0,0" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brush.SignalSurface}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Signal}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Signal}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="DensityAwareLedgerRow" TargetType="ListBoxItem" BasedOn="{StaticResource LedgerRow}">
        <Setter Property="MinHeight"
                Value="{Binding Source={x:Static settings:DisplaySettingsSource.Current}, Path=LedgerRowHeight}" />
    </Style>
    <Style x:Key="DensityAwareDeviceRow" TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
        <Setter Property="MinHeight"
                Value="{Binding Source={x:Static settings:DisplaySettingsSource.Current}, Path=LedgerRowHeight}" />
    </Style>
    <Style x:Key="AdaptiveRecordRow" TargetType="ListBoxItem" BasedOn="{StaticResource LedgerRow}">
        <Setter Property="MinHeight" Value="48" />
    </Style>
    <Style x:Key="InspectorSection" TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderDefault}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="0,0,0,16" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>
    <Style x:Key="KeyValueLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
        <Setter Property="FontSize" Value="{DynamicResource Type.Caption}" />
    </Style>
    <Style x:Key="KeyValueValue" TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
</ResourceDictionary>
